## Simulate 'x' users
number.of.threads=5
## The 'y' gap between each request for each user (ie x requests per y seconds)
ramp.up.period.in.seconds=0
## The 'z' requests per thread (ie Total requets = x * z)
loop.count=40
abort.after.time.lapsed.in.seconds=600